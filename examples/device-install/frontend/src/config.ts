// The example backend proxies the registry relay WebSockets with the Limrun
// API key attached server-side, so the frontend points `registryApiUrl` here
// instead of at https://registry.limrun.com directly.
export const BACKEND_URL = 'http://localhost:3000';
