// The example backend mints short-lived scoped registry tokens with the
// Limrun API key kept server-side; the browser then talks to the registry
// directly with that token.
export const BACKEND_URL = 'http://localhost:3000';
